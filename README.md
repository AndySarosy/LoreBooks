# LoreBooks

Angular starter for GitHub Pages.

## Local dev

1. Install Node.js 22 or newer.
2. Run `npm install`.
3. Run `npm start`.

## Build for Pages

Run `npm run deploy` to produce a production build with the `/LoreBooks/` base href.

GitHub Actions deploys the built app from `dist/lorebooks` on push to `main`.
